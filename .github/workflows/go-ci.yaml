
on:
  workflow_call:
    inputs:
      app:
        required: true
        type: string


jobs:
  check_test_build:
    strategy:
      matrix:
        go: [ 1.17 ]
        include:
          - os: ubuntu-latest
            go-build: ~/.cache/go-build
    name: ${{ matrix.go }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    steps:

      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}

      - name: Checkout Code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}

      - name: Info
        run: make info

      - name: List Dev Deps
        run: make go/dev-deps-file

      - uses: actions/cache@v2
        with:
          path: |
            ${{ matrix.go-build }}
            ~/go/pkg/mod
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}-${{ hashFiles('./out/dev_deps.txt') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - uses: actions/cache@v2
        with:
          path: |
            ./vendor
          key: ${{ runner.os }}-go_vendor-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install Dev Deps
        run: GOBIN=~/go/bin make go/dev-deps

      - name: Vendor
        run: make go/vendor

      - name: Check
        run: make go/check

      - name: Check Align
        run: make go/align

      - name: Check Format
        run: make go/fmt

      - name: Check Lint
        run: make go/lint

      - name: Run Tests
        run: make go/test

      - name: Build
        run: make go/build/${{ inputs.app }}

      - uses: actions/upload-artifact@v2
        with:
          name: out-${{ matrix.os }}-${{ matrix.go }}
          path: ./out/
